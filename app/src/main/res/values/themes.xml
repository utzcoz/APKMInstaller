<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No-op theme: Compose handles all theming -->
    <style name="Theme.APKMInstaller" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="Theme.APKMInstaller.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.APKMInstaller</item>
    </style>
</resources>
